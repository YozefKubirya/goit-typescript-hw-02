export interface Item{
   id:string,
   urls: {
     small: string;
     regular: string;
   };
   }
   export interface CurrentImageModalProps {
    id: string;
    urls: {
      regular: string;
    };
  }